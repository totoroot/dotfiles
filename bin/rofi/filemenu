#!/usr/bin/env sh
cd $HOME
DIR="$(fd -L -d 4 --type d . | rofi -dmenu -i -p "~/" -theme $DOTFILES/config/rofi/filemenu.rasi)"
if [ -d "$DIR" ]; then
  cd "$DIR"
  if [[ $1 == "-x" ]]; then
    xst
  else
    thunar
  fi
fi
